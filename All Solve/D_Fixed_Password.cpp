#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    while(true)
    {
        int n; cin>>n;
        if(n==1999)
        {
            cout<<"Correct";
            break;
        }
        cout<<"Wrong\n";
    }

    return 0;
}